# SPECTRA Configuration
# Copy to .env if needed (defaults work fine)

# Ollama (local LLM) - default: http://localhost:11434
# OLLAMA_HOST=http://localhost:11434

# CORS origins - default: * (allow all)
# ALLOWED_ORIGINS=*

